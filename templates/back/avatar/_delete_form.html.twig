<form method="post" action="{{ path('app_back_avatar_delete', {'id': avatar.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ avatar.id) }}">
    <button class="btn btn-danger mx-2" style="background-color: #d53767;border:none;color: white;font-weight:bold;">Supprimer</button>
</form>
</div>